describe( 'person section', function() {
    beforeEach( module( 'ngBoilerplate.person' ) );

    it( 'should have a dummy test', inject( function() {
        expect( true ).toBeTruthy();
    }));
});
